import { RouteRegistrar } from "./types";

// Placeholder until the Express adapter is wired through this registry.
export const expressRouteRegistrar: RouteRegistrar = () => {
  throw new Error("Express route registrar not implemented yet.");
};
