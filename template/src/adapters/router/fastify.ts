import { RouteRegistrar } from "./types";

// Placeholder until the Fastify adapter is wired through this registry.
export const fastifyRouteRegistrar: RouteRegistrar = () => {
  throw new Error("Fastify route registrar not implemented yet.");
};
